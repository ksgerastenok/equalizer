---------------------------------------------------------
mono   =     (L + R) / 2.0
stereo = w * (L - R) / 2.0
L = mono + stereo
R = mono - stereo
---------------------------------------------------------
s = (L + R) / 2.0
L = L + (w - 1.0) * (L - s) = s - w * (s - L)
R = R + (w - 1.0) * (R - s) = s - w * (s - R)
---------------------------------------------------------
s = Avg(c)
c[i] = c[i] + (w - 1.0) * (c[i] - s) = s - w * (s - c[i])
---------------------------------------------------------
